<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Navbar Fix</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Ensure proper visibility for collapsed navbar */
    @media (max-width: 767px) { /* Bootstrap breakpoint for md */
      .navbar-collapse {
        background-color: rgba(184, 204, 206, 0.9); /* Dark background for small screens */
      }
    }
    @media (min-width: 768px) {
      .navbar-collapse {
        background-color: transparent; /* Remove black background for desktop */
      }
    }
    .navbar-nav .nav-link {
      color: black; /* White text color for better contrast */
    }

    .navbar-nav .nav-link:hover {
      color: #00bcd4; /* Add a hover effect (optional) */
    }

    .dropdown-menu {
      background-color: #343a40; /* Dark background for dropdown */
      color: black; /* White text in dropdown items */
    }

    .dropdown-menu .dropdown-item {
      color: black; /* Ensure text color in dropdown items is visible */
    }

    .dropdown-menu .dropdown-item:hover {
      background-color: #495057; /* Slightly lighter background on hover */
      color: #00bcd4; /* Highlighted text color on hover */
    }
  </style>

<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top" >
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><i class="fa-solid fa-van-shuttle"><span>airbnb</span></i></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item">
          
          </li>
          
        </ul>
        <form class="d-flex" role="search">
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
        <div class="navbar-nav me-space-between">
          <a class="nav-link" href="/listing/new">Add New List</a>
          <%if(!currUser){%>
            <a class="nav-link" href="/signup"><b>Sign up</b></a>
            <a class="nav-link" href="/login"><b>Login</b></a>
          <%}%>
          <%if(currUser){%>
            <a class="nav-link" href="/logout"><b>Logout</b></a>
          <%}%>
        </div>
      </div>
    </div>
  </nav>